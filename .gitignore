/build/
/build-mac/
/build-mac-uni/
# AppImage build tree (scripts/package-appimage.sh)
/build-appimage/
# staged AppDir for linuxdeploy
/AppDir/
# cached linuxdeploy + plugin AppImages + stubs
/scripts/.tools/
*.AppImage
# from-source deps (static TagLib, etc.) — see scripts/
/.deps/
*.user
.cache/
compile_commands.json
tools/
testdata/
# Compiled shaders — baked by qt_add_shaders at build time; the demo's live
# reload writes to a temp dir, so none should be tracked under demo/.
demo/*.frag.qsb
demo/*.vert.qsb

# Generated probes / fixtures (derived from a local music library)
dates.tsv
tests/year_cases.tsv
notes/fixins.md
notes/fixins_new.md
notes/new_remote_sources.md
.DS_Store
aqtinstall.log

# Local secrets (e.g. .secret INI with a Subsonic/Navidrome server for testing)
.secret
*.secret
